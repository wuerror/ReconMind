## 报告输出

你正在执行最后阶段。读取 state.json 中的所有数据，生成最终报告。

### 生成文件

1. **output/target_report.md** — 完整侦察报告，结构如下：

```
# [公司名] 被动信息收集报告

## 目标概览
- 公司名称、子公司列表
- 已知域名、IP范围

## 攻击面摘要
- 总计发现：X 个域名、X 个子域名、X 个IP、X 个Web服务
- 高价值目标 TOP 10（按攻击价值排序，附理由）

## 详细发现

### 企业信息
（ENScan 结果，重点标注子公司和关联域名）

### 敏感信息泄露
（Google Dork 和 GitHub 搜索发现，按严重程度排序）

### 子域名清单
（按价值分级标注）

### 网络空间资产
（FOFA 发现，按端口/服务类型分类）

### Web 指纹识别
（按🔴🟡🟢三级分类展示）

## 建议攻击路径
基于以上发现，列出 3-5 条最可能的攻击路径，例如：
1. VPN 文档泄露 → 尝试凭据登录
2. Jenkins 未授权 → 代码执行
3. Spring Boot Actuator 暴露 → 信息泄露 → 进一步利用
```

2. **output/subdomain.txt** — 确认已是最新的去重子域名列表
3. **output/url.txt** — 确认已是最新的去重 URL 列表

### 红队思维

报告的核心价值不是罗列数据，而是 **建议攻击路径**。站在攻击者角度，基于所有发现，推断最可能成功的突破口。

### 完成标志

三个文件均已写入。输出报告文件路径后结束。